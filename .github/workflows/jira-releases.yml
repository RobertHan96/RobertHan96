name: "태스크4: Jira 릴리즈 일정 보고"

on:
  schedule:
    # 평일 09:00 KST = UTC 00:00 (월~금)
    - cron: "0 0 * * 1-5"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  jira-releases:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r scripts/automation/requirements.txt

      - name: Run Jira release report
        working-directory: scripts/automation
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          JIRA_BASE_URL: ${{ secrets.JIRA_BASE_URL }}
          JIRA_EMAIL: ${{ secrets.JIRA_EMAIL }}
          JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
          JIRA_PROJECT_KEY: ${{ secrets.JIRA_PROJECT_KEY }}
          TELEGRAM_MEMORY_ENABLED: "true"
          TELEGRAM_MEMORY_SOURCE: "jira-releases"
        run: python jira_releases.py

      - name: Commit telegram memory
        if: always()
        run: bash scripts/automation/commit_telegram_memory.sh "data/telegram_memory" "Update telegram memory"
